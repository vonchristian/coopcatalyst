<%=simple_form_for [@cooperative, @department], url: :cooperative_departments do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.submit "Create Department", class: "btn btn-primary btn-flat" %>
<% end %>
